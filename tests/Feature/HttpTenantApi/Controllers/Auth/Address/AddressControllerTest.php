<?php

declare(strict_types=1);

uses()->group('customer');

beforeEach(function () {
    testInTenantContext();
});

todo('can list');
todo('can create');

it('can create then replace new default', function () {

})
    ->with(['shipping', 'billing']);

todo('can edit');
todo('can delete');
